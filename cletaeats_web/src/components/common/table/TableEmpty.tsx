const TableEmpty = () => {
    return (
        <div className="flex items-center justify-center py-12 text-sm text-[var(--texto-sec)]">
            No hay registros disponibles
        </div>
    )
}

export default TableEmpty